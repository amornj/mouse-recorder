<Window x:Class="MouseRecorder.Views.OverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="#40000000"
        Topmost="True"
        WindowState="Maximized"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Cursor="Cross"
        MouseMove="Window_MouseMove"
        MouseLeftButtonDown="Window_MouseLeftButtonDown"
        KeyDown="Window_KeyDown">

    <Canvas x:Name="Overlay">
        <!-- Crosshair lines -->
        <Line x:Name="HLine" Stroke="#FFFF4444" StrokeThickness="1" />
        <Line x:Name="VLine" Stroke="#FFFF4444" StrokeThickness="1" />

        <!-- Coordinate label -->
        <Border x:Name="CoordBorder" Background="#CC000000" CornerRadius="3" Padding="6,3">
            <TextBlock x:Name="CoordText" Foreground="White" FontSize="14" FontFamily="Consolas" />
        </Border>

        <!-- Instructions -->
        <Border Canvas.Left="20" Canvas.Top="20" Background="#CC000000"
                CornerRadius="4" Padding="12,8">
            <TextBlock Foreground="White" FontSize="16">
                Click to capture position. Press Escape to cancel.
            </TextBlock>
        </Border>
    </Canvas>
</Window>
